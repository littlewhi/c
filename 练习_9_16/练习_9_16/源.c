#define _CRT_SECURE_NO_WARNINGS
//---大写变小写
//if (arr[i] >= 65 && arr[i] < 90)//判断是否在A与Z之间，Z单独处理
//{
//	arr[i] = arr[i] + '!';//大小写之间的ascii码值差了32，又因为还要向后移一位，所以+33
//}
//if (arr[i] == 'Z')
//{
//	arr[i] = 'a';
//}
